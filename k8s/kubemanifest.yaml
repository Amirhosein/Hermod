apiVersion: apps/v1
kind: Deployment
metadata:
  name: broker
  labels:
    app.kubernetes.io/name: amir-broker
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: amir-broker
  template:
    metadata:
      labels:
        app.kubernetes.io/name: amir-broker
    spec:
      containers:
        - name: amir-broker
          image: amirgames47/broker:latest
          ports:
            - name: prometheus
              containerPort: 8000
            - name: grpc
              containerPort: 8080

---
apiVersion: v1
kind: Service
metadata:
  name: amir-broker-service
  labels:
    app.kubernetes.io/name: amir-broker
spec:
  type: NodePort
  ports:
    - name: prometheus
      port: 8000
      nodePort: 30002
      targetPort: 8000
      protocol: TCP
    - name: grpc
      port: 8080
      nodePort: 30001
      targetPort: 8080
      protocol: TCP
  selector:
    app.kubernetes.io/name: amir-broker

